export default function SendResetLinkButton() {
    return (
        <button
            type="submit"
            className="cursor-pointer mt-5 w-full bg-blue-500 hover:bg-blue-700 py-2.5 rounded-lg font-semibold transition-colors text-neutral-100"
        >
            Send Reset Link
        </button>
    );
}